export default function UploadPage({ params: {id}}: {params: {id: string}}) {
  <>
      <section className="bg-black bg-dotted-pattern bg-cover bg-center py-5 md:py-10">
        <h3 className="wrapper h3-bold text-center sm:text-left text-black">Update Event</h3>
      </section>

      <div className="wrapper my-8">
        {/* <UpdateForm 
          eventId={id}
        /> */}
      </div>
    </>
}